name: Deploy to Subdomain (sales.shiphit.com)

on:
  push:
    branches:
      - salesv4-db-updated   # only runs when pushing to this branch

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Build project
        run: npm run build

      # Upload dist folder to Hostinger
      - name: Upload dist to Hostinger
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.HOSTINGER_HOST }}
          username: u331486718
          key: ${{ secrets.HOSTINGER_SSH_KEY }}
          port: 65002
          source: "dist/*"
          target: "/home/u331486718/domains/shiphit.com/public_html/sales"

      # (Optional) Clean old files before upload
      - name: Clean sales folder before upload
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.HOSTINGER_HOST }}
          username: u331486718
          key: ${{ secrets.HOSTINGER_SSH_KEY }}
          port: 65002
          script: |
            rm -rf /home/u331486718/domains/shiphit.com/public_html/sales/*
